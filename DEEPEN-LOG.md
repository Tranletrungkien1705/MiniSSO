# DEEPEN-LOG — MiniSSO

- 2026-XX-XX: BLOCKED — terminal delegation không khả dụng trong phiên này (mọi lệnh PowerShell/cmd/git-bash đều trả "Terminal delegation failed: terminated", không có output). Không thể đọc nguồn D:\idocNet\2017.A.iNOS.InBrand (read tool 403 ngoài workspace, glob chặn path ngoài workspace), không thể chạy dotnet build verify, không thể git commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry): BLOCKED lại — terminal delegation vẫn hỏng (thử PowerShell/cmd/git-bash, foreground/background, nhiều tên shell: tất cả "Terminal delegation failed: terminated", 0 output). Không đọc được nguồn ngoài workspace, không build verify, không git. Không port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 3): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo`, `Write-Output`, `Get-Location`, `cmd /c`, `& 'C:\Program Files\Git\bin\bash.exe' -lc ...`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 4): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo`, `Write-Output`, `Get-Date`, `whoami`, `cmd /c echo`, `$PSVersionTable`, `& 'C:\Program Files\Git\bin\bash.exe' -lc "echo probe"`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 5): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo hello`, `Write-Output "test-ok"`, `Get-Location`, `cmd /c echo cmd-ok`, `$PSVersionTable.PSVersion.ToString()`, `& 'C:\Program Files\Git\bin\bash.exe' -lc "echo bash-ok"`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-09-23: DONE — terminal đã hoạt động lại. Đọc nguồn iNOS.InBrand qua SVN working-copy (Dev/Dev20/Docs chỉ có .svn; đọc file từ pristine store bằng `_svndump` sau khi sửa bug đường dẫn pristine: tên file = full sha1, không tách 2 ký tự đầu). Nghiệp vụ port: **RBAC theo nhóm** (Sys_User → Sys_UserInGroup → Sys_Group → Sys_Access → Sys_Object) — MiniSSO trước chỉ có Roles csv phẳng. Thêm entities Group/GroupMember/PermissionObject/GroupAccess (Domain/Entities.cs), DbSets+index (Data/AppDbContext.cs), RbacService (quyền hiệu lực = hợp đối tượng quyền qua các nhóm đang hoạt động), seed 3 nhóm + 5 đối tượng quyền + cấp quyền + thành viên (Data/Seeder.cs, kèm DDL Postgres), API /api/v1/groups|objects|groups/{id}/access|members|users/{id}/permissions (ApiV1Controller), trang admin /Group (GroupController + Views/Group/Index.cshtml + nav). Build Release: 0 error (1 warning cũ không liên quan). Chạy thật + test: admin→SYSADMIN→5 quyền, sales→SALES→1 quyền; create group→grant→add member→quyền hiệu lực cập nhật đúng. Commit 2fe9f54 trên main (đã ở origin/main). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 6): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + _svndump). Nghiệp vụ port: **Bảo mật tài khoản / khoá đăng nhập** (SysUser: Enable / Lockout / LockoutDate / VerificationCode + Sys_User_Login + ResetPass). MiniSSO trước chỉ có IsActive, không đếm sai, không khoá. Thêm: AppUser.FailedLoginCount/IsLockedOut/LockoutDate/LockoutUntil/VerificationCode/LastLoginAt (Domain/Entities.cs) + entity LoginAttempt (nhật ký đăng nhập) + DbSet/index (Data/AppDbContext.cs); AccountSecurityService (đếm sai liên tiếp, tự khoá sau 5 lần sai trong 15 phút, tự mở khi hết hạn, admin lock/unlock/reset-password, ghi log mỗi lần thử); AuthService.ValidateUserAsync + AccountController.Login dùng service (hiện lý do cụ thể: vô hiệu/khoá/sai); API /api/v1/users/{id}/lock|unlock|reset-password + /api/v1/login-attempts + mở rộng GET /users (ApiV1Controller); admin UI UserController Lock/Unlock/ResetPassword + Views/User/Index.cshtml (cột Bảo mật + nút); Seeder thêm DDL Postgres (ALTER TABLE Users ADD COLUMN IF NOT EXISTS ... + bảng LoginAttempts). Build Release: 0 error (1 warning cũ không liên quan). Test: 15/15 pass (8 test mới cho lockout: đếm sai, khoá sau ngưỡng, từ chối khi khoá/vô hiệu, admin mở khoá, reset mật khẩu, ghi log). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 7): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + _svndump). Nghiệp vụ port: **Phạm vi dữ liệu theo nhánh tổ chức (ViewAbility)** — từ SysUserProvider: Sys_User_GetListAbilityViewOfUser + Mst_Dealer_ViewAbility_GetFromListUser + myCache_ViewAbility_CheckAccess(Dealer/ExactDealer/ExactUser/CheckDealerRoot). iNOS gắn user vào 1 đại lý (SysUser.DLCode) trong cây đại lý (materialized path DLBUCode/DLBUPattern/DLLevel); user chỉ thấy dữ liệu thuộc nhánh mình, trừ SysAdmin (thấy tất cả) hoặc ở nút gốc. MiniSSO trước chỉ có RBAC chức năng (nhóm→đối tượng quyền), KHÔNG có giới hạn dữ liệu theo nhánh. Thêm: AppUser.IsSysAdmin + AppUser.OrgId (Domain/Entities.cs) + index OrgId (Data/AppDbContext.cs); DataScopeService (VisibleOrgIdsAsync/VisibleOrgsAsync/CanAccessOrgAsync/IsExactOrgAsync/IsExactUserAsync/IsRootOrgAsync/CheckAccessAsync — tái tạo đúng logic trên cây Org sẵn có); API /api/v1/users/{id}/scope (GET/POST) + /scope/check + mở rộng GET /users (ApiV1Controller); admin UI DataScopeController + Views/DataScope/Index.cshtml + nav "Phạm vi dữ liệu" (_Layout); Seeder thêm DDL Postgres (ALTER TABLE Users ADD COLUMN IsSysAdmin/OrgId + index) + seed phạm vi (admin→SysAdmin, sales→Miền Bắc, dealer→Đại lý Đông Đô). Build Release: 0 error (1 warning cũ không liên quan). Test: 23/23 pass (8 test mới cho DataScope: SysAdmin thấy tất cả, user gốc thấy tất cả, user nhánh chỉ thấy nhánh con, user lá chỉ thấy chính nó, user không gắn đơn vị thấy rỗng, CanAccessOrg theo nhánh, IsExactOrg/IsExactUser, CheckAccess gộp cờ). Chạy thật: sales→2 đơn vị (0.10, 0.10.11), dealer→1 (0.20.21.211), admin→unrestricted 6 đơn vị; /scope/check HCM=false, North=true; trang /DataScope render OK. Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 8): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + _svndump, chạy bằng `dotnet _svndump.dll` vì apphost thiếu runtime). Nghiệp vụ port: **Phân hệ chức năng Module → Function + "menu hiệu lực" theo người dùng** — từ SysModule (cây qua ParentCode) / SysFunction / SysFunctionInModule (nối nhiều-nhiều) + SysModuleManager.GetAllByUser (nhóm đang hoạt động → SysAccess → module → kèm chức năng). MiniSSO trước chỉ có PermissionObject phẳng với 1 cột Module dạng chuỗi, KHÔNG có cây module/chức năng/menu. Thêm: entities Module/Function/FunctionInModule (Domain/Entities.cs) + DbSets/index (Data/AppDbContext.cs); ModuleService (AllWithFunctionsAsync, MenuForUserAsync = hợp module qua các nhóm đang hoạt động, SetFunctionsAsync thay-thế toàn bộ, SubtreeAsync); API /api/v1/modules (GET/POST) + /modules/{id}/subtree + PUT /modules/{id}/functions + GET /users/{id}/menu (ApiV1Controller); admin UI ModuleController + Views/Module/Index.cshtml + nav "Phân hệ chức năng" (_Layout); Seeder thêm DDL Postgres (Modules/Functions/FunctionInModules) + seed cây module (sso→user/group/org, license, report) + 6 chức năng + gắn chức năng vào module. Build Release: 0 error (1 warning cũ không liên quan). Test: 41/41 pass (9 test mới cho ModuleService: thay-thế chức năng, từ chối chức năng/module không tồn tại, cây kèm chức năng, menu rỗng khi không nhóm, hợp module qua nhiều nhóm, loại nhóm khoá, bỏ qua đối tượng không gắn module, subtree). Commit fa39bd7 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 9): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Nhóm cột hiển thị** (View_GroupView / View_ColumnInGroup / View_ColumnView + ViewColumnInGroupManager.ViewColumnInGroupSaveX). iNOS cho cấu hình "cột hiển thị" (View_ColumnView) và gom thành "nhóm cột hiển thị" (View_GroupView) qua bảng nối View_ColumnInGroup; khi lưu 1 nhóm dùng cơ chế "xoá sạch rồi ghi lại" (clear-all → insert-all), kiểm tra nhóm tồn tại & đang hoạt động, mỗi cột tồn tại & đang hoạt động, danh sách cột không rỗng. MiniSSO trước đây KHÔNG có khái niệm cấu hình cột hiển thị theo nhóm. Thêm: entities ViewColumnView/ViewGroupView/ViewColumnInGroup (Domain/Entities.cs) + DbSets/index (Data/AppDbContext.cs); ViewGroupService (AllWithColumnsAsync, ColumnsOfGroupAsync, SetColumnsAsync = thay-thế toàn bộ + validate, DeleteGroupAsync = xoá nhóm kèm dọn liên kết); API /api/v1/view-columns (GET/POST) + /view-columns/{id}/toggle + /view-groups (GET/POST) + /view-groups/{id}/toggle + PUT /view-groups/{id}/columns + DELETE /view-groups/{id} (ApiV1Controller); admin UI ViewGroupController + Views/ViewGroup/Index.cshtml + nav "Nhóm cột hiển thị" (_Layout); Seeder thêm DDL Postgres (ViewColumnViews/ViewGroupViews/ViewColumnInGroups) + seed 5 cột + 2 nhóm (GRID_SALE, GRID_ADMIN) + gán cột. Build Release: 0 error (1 warning cũ không liên quan). Test: 51/51 pass (10 test mới cho ViewGroupService: thay-thế toàn bộ, từ chối cột không tồn tại, từ chối cột bị khoá, từ chối danh sách rỗng, nhóm không tồn tại, nhóm bị khoá, AllWithColumns, ColumnsOfGroup sắp theo mã, xoá nhóm kèm dọn liên kết, xoá nhóm không tồn tại). Commit bb243d6 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 10): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Đội người dùng** (Sys_UserTeam: TeamCode PK, DLCode, TeamName, FlagActive + SysUserTeamManager/SysUserTeamProvider). iNOS có bảng "đội" thuộc một đại lý/đơn vị (DLCode) — khái niệm TỔ CHỨC khác với Group (nhóm quyền). MiniSSO trước đây KHÔNG có khái niệm "đội". Thêm: entity UserTeam (Domain/Entities.cs) + DbSet/index (Data/AppDbContext.cs); UserTeamService (AllAsync, ByOrgAsync, CreateAsync/UpdateAsync = validate mã đội bắt buộc & duy nhất + đơn vị phải tồn tại & đang hoạt động ↔ MstDealerCheckDB, ToggleAsync, DeleteAsync); API /api/v1/user-teams (GET/POST) + PUT /user-teams/{id} + /user-teams/{id}/toggle + DELETE /user-teams/{id} (ApiV1Controller); admin UI UserTeamController + Views/UserTeam/Index.cshtml + nav "Đội người dùng" (_Layout); Seeder thêm DDL Postgres (UserTeams + index Code/OrgId) + seed 3 đội (TEAM_HN1/TEAM_HN2→Miền Bắc, TEAM_DD1→Đại lý Đông Đô). Build Release: 0 error (1 warning cũ không liên quan). Test: 62/62 pass (11 test mới cho UserTeamService: tạo đội, mã bắt buộc, mã trùng, đơn vị không tồn tại, đơn vị bị khoá, tên mặc định = mã, cập nhật tên/đơn vị, cập nhật đội không tồn tại, toggle, xoá, lọc theo đơn vị). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 11): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Truy vấn thành viên nhóm** (SysUserProvider.GetAllUserByGroupCode + GetAllUserNotInGroup + SysUserInGroupProvider.RemoveByUser). iNOS dùng 2 truy vấn này cho màn hình "Gán người dùng vào nhóm" (SysGroupController.GetSysUser): liệt kê người dùng ĐANG thuộc 1 nhóm (để tích sẵn) và người dùng CHƯA thuộc bất kỳ nhóm nào; RemoveByUser gỡ 1 người khỏi mọi nhóm khi xoá người dùng. MiniSSO trước đây chỉ thêm/bớt/thay-thế thành viên (SetMember/SetMembersAsync) mà KHÔNG truy vấn được "ai đang trong nhóm". Thêm: GroupService.MembersOfGroupAsync (↔ GetAllUserByGroupCode, sắp theo email, rỗng nếu nhóm không tồn tại) + UsersNotInAnyGroupAsync (↔ GetAllUserNotInGroup) + RemoveUserFromAllGroupsAsync (↔ RemoveByUser, trả số liên kết đã gỡ) (Services/GroupService.cs); API GET /api/v1/groups/{id}/members + GET /api/v1/users-not-in-group + DELETE /api/v1/users/{id}/groups (ApiV1Controller); admin UI GroupMemberController + Views/GroupMember/Index.cshtml (chọn nhóm → xem thành viên + danh sách chưa thuộc nhóm + nút gỡ khỏi mọi nhóm) + nav "Thành viên nhóm" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 70/70 pass (8 test mới cho GroupMembershipTests: MembersOfGroup chỉ trả thành viên, nhóm không tồn tại → rỗng, sắp theo email, UsersNotInAnyGroup loại người đã thuộc nhóm, loại người thuộc nhóm khác, tất cả đã gán → rỗng, RemoveUserFromAllGroups gỡ mọi liên kết + giữ người khác, không có liên kết → 0). Commit 926820f trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 12): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Tự đổi mật khẩu (self-service change-password)** — từ AccountController.ChangePassword (web) + SysUserManager.ResetPassword. iNOS cho người dùng ĐANG ĐĂNG NHẬP tự đổi mật khẩu của chính mình: mật khẩu mới bắt buộc không rỗng, mật khẩu nhập lại phải khớp (passnew.Equals(confpass)), rồi gọi ResetPass(session.SysUser.Code, passnew) chỉ ghi cho chính user đó. MiniSSO trước đây CHỈ có đặt lại mật khẩu do ADMIN (AccountSecurityService.ResetPasswordAsync), KHÔNG có luồng người dùng tự đổi (phải xác thực mật khẩu hiện tại + xác nhận mật khẩu mới). Thêm: SelfServiceService (ChangeOwnPasswordAsync: user tồn tại & đang hoạt động, mật khẩu hiện tại phải khớp, mật khẩu mới không rỗng & ≥6 ký tự, nhập lại phải khớp, thành công thì xoá bộ đếm đăng nhập sai) (Services/SelfServiceService.cs); đăng ký DI (Program.cs); API POST /api/v1/users/{id}/change-password (ApiV1Controller + ChangePasswordReq); UI tự phục vụ AccountController.ChangePassword (GET/POST, [Authorize], lấy userId từ claim) + Views/Account/ChangePassword.cshtml + nav "Đổi mật khẩu" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 79/79 pass (9 test mới cho SelfServiceService: đổi thành công, sai mật khẩu hiện tại, nhập lại không khớp, mật khẩu mới rỗng, nhập lại rỗng, quá ngắn, user bị vô hiệu, user không tồn tại, xoá bộ đếm sai). Commit c1ed5c1 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 13): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Kiểm tra tồn tại/trạng thái trước khi lưu (mẫu "CheckDB")** — từ SysUserManager.SysUserCheckDB + SysGroupManager.SysGroupCheckDB + SysModuleManager.MstModuleCheckDB + MstDealerManager.MstDealerCheckDB (hằng cờ Constants.Flag: Active/Yes="1", Inactive/No="0"). iNOS dùng CÙNG một mẫu kiểm tra ở mọi manager trước khi ghi: strFlagExistToCheck ("1"=bản ghi PHẢI tồn tại, "0"=PHẢI chưa tồn tại) + strFlagActiveListToCheck (trạng thái thực tế "1"/"0" phải nằm trong danh sách cho phép), vi phạm thì ném ServiceException kèm mã lỗi (…_CheckDB_UserCodeNotFound / …_CodeExist / …_FlagActiveNotMatched). MiniSSO trước đây kiểm tra rải rác mỗi chỗ một kiểu (AnyAsync + thông báo tự do), KHÔNG có mẫu dùng chung. Thêm: CheckDbService (CheckAsync(kind, code, flagExistToCheck, flagActiveListToCheck) cho 4 loại User/Group/Module/Org + hằng FlagActive/FlagInactive + record CheckResult(Ok,Error,Exists,Status)) (Services/CheckDbService.cs); đăng ký DI (Program.cs); API GET /api/v1/checkdb?kind&code&exist&active (ApiV1Controller) + áp mẫu CheckDB vào các luồng tạo sẵn có (CreateUser/CreateGroup/CreateModule/CreateOrg dùng checkDb.CheckAsync(..., FlagInactive) thay cho AnyAsync thủ công); admin UI CheckDbController + Views/CheckDb/Index.cshtml (chọn loại + mã + cờ, hiện kết quả + mã gợi ý) + nav "Kiểm tra trước khi lưu" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 90/90 pass (11 test mới cho CheckDbService: phải tồn tại khi có/không có, phải chưa tồn tại khi có/không có, danh sách cờ cho phép nhận/loại bản ghi hoạt động/khoá, kết hợp cả hai điều kiện, mã rỗng coi như không tồn tại, không kiểm tra gì thì luôn hợp lệ). Commit c7e1828 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 14): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Gán module trực tiếp cho nhóm** (Sys_Access = GroupCode + ModuleCode) — từ SysGroupController.GetSysModule + SysAccessService.GetAllAccessByGroupCode + SysAccessManager.SysAccessSave_New20171101. iNOS nối TRỰC TIẾP nhóm ↔ module qua bảng Sys_Access; màn hình "Gán module vào nhóm" liệt kê TẤT CẢ module kèm cờ "đã gán cho nhóm này chưa", lưu theo cơ chế "xoá sạch rồi ghi lại" (clear-all → insert-all), kiểm tra nhóm tồn tại & đang hoạt động + mỗi module tồn tại (MstModuleCheckDB). MiniSSO trước đây chỉ có GroupAccess (nhóm ↔ đối tượng quyền) và suy ra module GIÁN TIẾP qua PermissionObject.Module — KHÔNG có liên kết nhóm ↔ module trực tiếp. Thêm: entity GroupModuleAccess (Domain/Entities.cs) + DbSet/index (Data/AppDbContext.cs); ModuleService.GrantedModuleCodesAsync (↔ GetAllAccessByGroupCode) + ModulesForGroupAsync (tất cả module kèm cờ Granted) + SetGroupModulesAsync (↔ SysAccessSave_New20171101, thay-thế toàn bộ + validate) + record ModuleGrant (Services/ModuleService.cs); API GET /api/v1/groups/{id}/modules + GET /groups/{id}/module-codes + PUT /groups/{id}/modules (ApiV1Controller + GroupModulesReq); admin UI GroupModuleController + Views/GroupModule/Index.cshtml (chọn nhóm → tick module → lưu) + nav "Gán module vào nhóm" (_Layout); Seeder thêm DDL Postgres (GroupModuleAccesses + unique index) + seed (SYSADMIN→toàn bộ module, SALES→report). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 99/99 pass (9 test mới cho GroupModuleServiceTests: thay-thế toàn bộ, danh sách rỗng xoá hết, từ chối module không tồn tại, nhóm không tồn tại, GrantedModuleCodes rỗng khi nhóm lạ, sắp theo mã, ModulesForGroup gắn cờ đúng, nhóm lạ rỗng, khử trùng mã). Commit 8b057ec trên main (đã ở origin/main). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 15): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Cập nhật hồ sơ người dùng theo danh sách cột cho phép** (mẫu "Ft_Cols_Upd") — từ SysUserManager.SysUserUpdateX / SysUserUpdateX_New20190917. iNOS KHÔNG ghi toàn bộ bản ghi khi sửa người dùng: tầng trên truyền chuỗi objFt_Cols_Upd liệt kê cột được phép ghi (vd "Sys_User.Email, Sys_User.FullName, Sys_User.Enable, ..."); manager đọc bản ghi hiện có rồi CHỈ gán lại các cột có trong danh sách (bUpd_Email/bUpd_FullName/…), cột khác giữ nguyên — nhờ vậy nhiều màn hình sửa các tập trường khác nhau trên cùng 1 user mà không ghi đè lẫn nhau. MiniSSO trước đây chỉ có bật/tắt (IsActive), khoá/mở khoá, đặt lại mật khẩu, gắn phạm vi — KHÔNG có luồng "sửa hồ sơ" tổng quát. Thêm: UserProfileService (UpdateAsync(userId, UserProfilePatch, columns) — danh sách trắng UpdatableColumns = Email/FullName/Roles/Tenant/IsActive/IsSysAdmin/OrgId; rỗng = tất cả; kiểm tra user tồn tại ↔ SysUserCheckDB, email không rỗng & không trùng người khác, đơn vị phải tồn tại & đang hoạt động ↔ MstDealerCheckDB) + record UserProfilePatch (Services/UserProfileService.cs); đăng ký DI (Program.cs); API PUT /api/v1/users/{id} (ApiV1Controller + UserUpdateReq có Columns); admin UI UserController.Update + form "Sửa" (collapse) trong Views/User/Index.cshtml (tick cột nào ghi cột đó) + ViewBag.Orgs. Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 112/112 pass (13 test mới cho UserProfileService: chỉ ghi cột được liệt kê, rỗng = tất cả, user không tồn tại, email chuẩn hoá lowercase, email rỗng bị từ chối, email trùng bị từ chối, giữ nguyên email của chính mình, Org hợp lệ, Org không tồn tại, Org bị khoá, cờ chỉ đổi khi được liệt kê, cờ đổi khi được liệt kê, cột ngoài danh sách trắng bị bỏ qua). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 16): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Cập nhật hồ sơ NHÓM theo danh sách cột cho phép (mẫu "Ft_Cols_Upd") + truy vấn nhóm của 1 người dùng** — từ SysGroupManager.SysGroupUpdateX (objFt_Cols_Upd = "Sys_Group.Description, Sys_Group.Enable, Sys_Group.FlagActive"; chỉ gán lại các cột có trong danh sách: bUpd_Description/bUpd_Enable/bUpd_FlagActive, cột khác giữ nguyên) + SysGroupProvider.GetAllByUser (SELECT g.* FROM SysGroup g INNER JOIN SysUserInGroup uig ON g.Code=uig.GroupCode AND uig.UserCode=@UserCode). MiniSSO trước đây đã có UserProfileService (cập nhật-theo-cột cho NGƯỜI DÙNG) nhưng KHÔNG có bản tương ứng cho NHÓM (chỉ có bật/tắt IsActive + tạo nhóm), và RbacService.GroupCodesAsync chỉ trả mã nhóm chứ không trả danh sách nhóm đầy đủ. Thêm: GroupProfileService (UpdateAsync(groupId, GroupProfilePatch, columns) — danh sách trắng UpdatableColumns = Name/Description/IsActive/OrgId; rỗng = tất cả; kiểm tra nhóm tồn tại ↔ SysGroupCheckDB, đơn vị phải tồn tại & đang hoạt động ↔ MstDealerCheckDB; tên rỗng → lấy lại mã nhóm) + GroupsOfUserAsync (↔ GetAllByUser, sắp theo mã, rỗng nếu user không tồn tại/không thuộc nhóm) + record GroupProfilePatch (Services/GroupProfileService.cs); đăng ký DI (Program.cs); API PUT /api/v1/groups/{id} (ApiV1Controller + GroupUpdateReq có Columns) + GET /api/v1/users/{id}/groups; admin UI GroupController.Update + form "Sửa" (collapse) trong Views/Group/Index.cshtml (tick cột nào ghi cột đó). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 126/126 pass (14 test mới cho GroupProfileServiceTests: chỉ ghi cột được liệt kê, rỗng = tất cả, nhóm không tồn tại, tên rỗng → mã nhóm, Org hợp lệ, Org không tồn tại, Org bị khoá, cờ chỉ đổi khi được liệt kê, cờ đổi khi được liệt kê, cột ngoài danh sách trắng bị bỏ qua, GroupsOfUser chỉ trả nhóm đã tham gia, user không tồn tại → rỗng, không thuộc nhóm → rỗng, sắp theo mã). Commit e64f971 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 17): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Xoá người dùng kèm dọn liên kết nhóm** — từ SysUserManager.Remove → SysUserDeleteX + SysUserInGroupProvider.RemoveByUser. iNOS KHÔNG xoá thẳng dòng Sys_User: `Remove` mở transaction, gọi `SysUserDeleteX` kiểm tra người dùng PHẢI tồn tại (SysUserCheckDB với strFlagExistToCheck = "1") rồi xoá bản ghi; bản `Remove` cũ (đang bị comment trong nguồn) còn gỡ người dùng khỏi MỌI nhóm trước khi xoá (uigMng.RemoveByUser → DELETE FROM SysUserInGroup WHERE UserCode = @userCode). MiniSSO trước đây chỉ có GroupService.RemoveUserFromAllGroupsAsync (gỡ khỏi nhóm) nhưng KHÔNG có luồng xoá người dùng. Thêm: UserDeleteService (DeleteAsync: kiểm tra tồn tại ↔ SysUserCheckDB, gỡ khỏi mọi nhóm ↔ RemoveByUser, xoá người dùng; trả UserDeleteResult kèm số liên kết nhóm đã dọn) (Services/UserDeleteService.cs); đăng ký DI (Program.cs); API DELETE /api/v1/users/{id} (ApiV1Controller); admin UI UserController.Delete + nút xoá (icon thùng rác, có confirm) trong Views/User/Index.cshtml. Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 130/130 pass (4 test mới cho UserDeleteService: xoá người dùng, xoá kèm dọn liên kết nhóm + đếm đúng, không có liên kết → 0, người dùng không tồn tại → fail + không xoá nhầm ai). Commit 1d33a3b trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 18): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Nhập hàng loạt nhóm người dùng từ file** — từ SysGroupController.Import (web) + SysGroupService.GetAll/Add. iNOS cho phép nhập nhiều nhóm từ file Excel (đọc từ ô "A2") và kiểm tra TOÀN BỘ file TRƯỚC KHI ghi bất kỳ dòng nào: file phải đúng định dạng + đúng 3 cột (Code, DLCode, Description); mỗi dòng Code/DLCode/Description không được trống; Description ≤ ParamsClient.RemarkLength (=400) ký tự; Code không được lặp trong chính file; nếu mọi dòng hợp lệ mới thêm từng nhóm với Enable=true. MiniSSO trước đây chỉ tạo nhóm ĐƠN LẺ (GroupController.Create), KHÔNG có luồng nhập hàng loạt. Thêm: GroupImportService (ImportCsvAsync: tái tạo đúng các quy tắc kiểm tra của iNOS — đúng số cột, không rỗng, mô tả ≤ 400, mã không lặp trong file, mã đã tồn tại trong DB; DLCode khớp Org.Code → gán OrgId, không khớp → nhóm toàn cục; mã chuẩn hoá UPPER; phần đọc file dùng CSV thay Excel vì MiniSSO không kèm thư viện Excel — quy tắc nghiệp vụ giữ nguyên, chỉ khác định dạng đầu vào) + hằng RemarkLength/RequiredColumns + record GroupImportResult (Services/GroupImportService.cs); đăng ký DI (Program.cs); API POST /api/v1/groups/import (ApiV1Controller + GroupImportReq); admin UI GroupImportController + Views/GroupImport/Index.cshtml (textarea nhập CSV + bảng nhóm hiện có) + nav "Nhập nhóm từ file" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 144/144 pass (14 test mới cho GroupImportServiceTests: thêm tất cả dòng, map DLCode→Org, DLCode lạ→OrgId null, nội dung rỗng, chỉ có tiêu đề, sai số cột, mã rỗng, DLCode rỗng, mô tả rỗng, mô tả quá dài, mô tả đúng giới hạn, mã lặp trong file, mã đã có trong DB, chuẩn hoá mã UPPER). Commit 4362633 trên main (đã ở origin/main). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 19): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Tìm kiếm có phân trang + lọc phạm vi dữ liệu** — từ SysUserManager.Search + SysGroupManager.Search. iNOS KHÔNG trả về toàn bộ bảng khi màn hình quản trị tìm kiếm: mỗi lần gọi Search truyền (searchDic, pageIndex, pageSize, orderByColums) và nhận về 1 TRANG kết quả (PageInfo); trước khi truy vấn chèn điều kiện phạm vi dữ liệu — nếu người gọi là SysAdmin HOẶC ở nút gốc (DLCode = DLCodeRoot) thì KHÔNG giới hạn, ngược lại chỉ thấy bản ghi có DLCode nằm trong danh sách đại lý mà người gọi được thấy (searchDic.Add("SysUser.DLCode in", lstMstDealer)); sau khi lấy trang, SysUserManager.Search còn GẮN NHÓM cho từng người dùng (item.Groups). MiniSSO trước đây chỉ có danh sách phẳng (db.Users.ToListAsync) — KHÔNG phân trang, KHÔNG lọc phạm vi, KHÔNG gắn nhóm. Thêm: SearchService (SearchUsersAsync/SearchGroupsAsync: phân trang + lọc phạm vi qua DataScopeService.VisibleOrgIdsAsync — SysAdmin/nút gốc → tất cả, ngược lại chỉ nhánh đơn vị; nhóm toàn cục OrgId=null luôn hiển thị; từ khoá khớp email/tên hoặc mã/tên không phân biệt hoa thường; lọc trạng thái; sắp xếp; gắn nhóm cho từng người dùng) + record PagedResult<T>/UserSearchRow + hằng DefaultPageSize (Services/SearchService.cs); đăng ký DI (Program.cs); API GET /api/v1/search/users + /api/v1/search/groups (ApiV1Controller); admin UI SearchController + Views/Search/Index.cshtml (chọn người gọi → thấy kết quả bị lọc theo phạm vi + phân trang + gắn nhóm) + nav "Tìm kiếm phân trang" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 162/162 pass (18 test mới cho SearchServiceTests: SysAdmin thấy tất cả, phân trang không trùng, sắp theo email, người gọi nhánh chỉ thấy nhánh mình, người gọi lá chỉ thấy chính mình, người gọi nút gốc thấy tất cả, người gọi không đơn vị thấy rỗng, từ khoá khớp email/tên, không phân biệt hoa thường, lọc trạng thái, gắn nhóm, không nhóm → rỗng, tìm nhóm SysAdmin thấy tất cả, người gọi nhánh thấy nhóm cùng đơn vị + toàn cục, loại nhóm đơn vị khác, từ khoá nhóm, phân trang nhóm, pageSize kẹp về mặc định). Commit de8eee6 trên main (đã ở origin/main, xác nhận bằng git merge-base --is-ancestor). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 20): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Xuất danh sách nhóm ra file** — từ SysGroupController.Export + ExportTemplate + GetImportDicColums/GetImportDicColumsTemplate. iNOS cho xuất Excel theo 2 chế độ: (1) dữ liệu thật — mỗi nhóm gồm cột Code, DLCode, Description, Enable (GetImportDicColums), lấy từ SysGroupService.GetAll(sessionId, "Code, Description, Enable"); (2) file MẪU rỗng chỉ có tiêu đề Code, DLCode, Description (GetImportDicColumsTemplate) để điền rồi nhập lại. MiniSSO trước đây chỉ có luồng NHẬP (GroupImportService) mà KHÔNG có luồng XUẤT tương ứng. Thêm: GroupExportService (ExportCsvAsync = toàn bộ nhóm sắp theo mã, DLCode = Org.Code hoặc rỗng nếu nhóm toàn cục, Enable = 1/0 ↔ TConst.Flag.Yes/No, có bọc ô CSV chứa dấu phẩy/nháy kép; ExportTemplateCsv = chỉ dòng tiêu đề; hằng DataHeaders/TemplateHeaders) (Services/GroupExportService.cs); đăng ký DI (Program.cs); API GET /api/v1/groups/export + /api/v1/groups/export-template (trả file text/csv) (ApiV1Controller); admin UI GroupExportController + Views/GroupExport/Index.cshtml (2 nút tải + xem trước dữ liệu xuất) + nav "Xuất nhóm ra file" (_Layout). Phần "ghi file" dùng CSV thay Excel vì MiniSSO không kèm thư viện Excel — tập cột + tiêu đề giữ nguyên, chỉ khác định dạng (giống cách GroupImportService đã làm). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 170/170 pass (8 test mới cho GroupExportServiceTests: DB rỗng chỉ có tiêu đề, tiêu đề khớp DataHeaders, xuất đủ nhóm sắp theo mã, map OrgId→DLCode, nhóm toàn cục DLCode rỗng, nhóm khoá Enable=0, mô tả chứa dấu phẩy được bọc nháy kép, file mẫu chỉ có tiêu đề không Enable). Commit db90a0b trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 21): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Truy vấn người dùng theo đơn vị & theo trạng thái hoạt động** — từ SysUserService.GetByDLCode + GetAllByEnable (clientservice) + SysUserManager.Search (searchDic "SysUser.DLCode =" / "SysUser.Enable ="). iNOS KHÔNG có 1 hàm "lấy tất cả người dùng": mỗi màn hình gọi truy vấn có điều kiện — GetByDLCode liệt kê người dùng thuộc MỘT đại lý/đơn vị (kèm GẮN NHÓM qua ReturnTableDict SysUser+SysGroup), GetAllByEnable lọc theo cờ hoạt động (màn hình "Gán người dùng vào nhóm" SysGroupController.GetSysUser chỉ liệt kê người ĐANG hoạt động, enable="True"). MiniSSO trước đây chỉ có danh sách phẳng (db.Users.ToListAsync) và truy vấn theo NHÓM (GroupService.MembersOfGroupAsync/UsersNotInAnyGroupAsync) — KHÔNG có truy vấn theo ĐƠN VỊ và KHÔNG lọc theo trạng thái. Thêm: UserQueryService (ByOrgAsync ↔ GetByDLCode, rỗng nếu đơn vị không tồn tại, sắp theo email, kèm nhóm của từng người; ByActiveAsync ↔ GetAllByEnable, true=đang hoạt động/false=bị vô hiệu/null=tất cả) (Services/UserQueryService.cs); đăng ký DI (Program.cs); API GET /api/v1/users/by-org/{orgId} + GET /api/v1/users/by-active?active= (ApiV1Controller); admin UI UserQueryController + Views/UserQuery/Index.cshtml (chọn đơn vị → xem người dùng kèm nhóm + bảng lọc theo trạng thái) + nav "Người dùng theo đơn vị" (_Layout). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 178/178 pass (8 test mới cho UserQueryServiceTests: ByOrg chỉ trả người của đơn vị, đơn vị lạ → rỗng, sắp theo email, gắn nhóm đúng; ByActive mặc định chỉ người hoạt động, false chỉ người vô hiệu, null tất cả, sắp theo email). Commit dd646c6 trên main (đã ở origin/main, xác nhận bằng git ls-remote). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-24 (retry 22): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + `dotnet _svndump.dll`; Dev20/Docs rỗng, không có task BRAIN nào trong nguồn). Nghiệp vụ port: **Tự đăng ký tham gia hệ thống (Join/Register)** — từ AccountController.Join (web) + AccountService.Register + SysUserManager.Register → SysUserAddX_New20190917. iNOS cho phép một người dùng (đã xác thực qua OAuth bên ngoài) tự hoàn tất hồ sơ để trở thành người dùng hệ thống (màn hình Account/Join): điền Code/FullName/Email rồi gọi Register → Add. Khi thêm, SysUserAddX_New20190917 áp quy tắc: Code bắt buộc & PHẢI CHƯA tồn tại (SysUserCheckDB strFlagExistToCheck="0"), DLCode tuỳ chọn nhưng nếu có phải tồn tại & đang hoạt động (MstDealerCheckDB), Email/FullName/PhoneNo/VerificationCode được Trim, Lockout=false, Enable=true, Language="vi", LockoutDate=now, mật khẩu băm trước khi lưu. MiniSSO trước đây CHỈ có luồng ADMIN tạo người dùng (ApiV1Controller.CreateUser / UserController.Create), KHÔNG có luồng người dùng TỰ đăng ký. Thêm: AppUser.PhoneNo + AppUser.Language (mặc định "vi") (Domain/Entities.cs); RegistrationService (RegisterAsync: email bắt buộc & chưa tồn tại ↔ SysUserCheckDB FlagInactive, mật khẩu bắt buộc & ≥6 ký tự, đơn vị nếu có phải tồn tại & đang hoạt động ↔ MstDealerCheckDB, tạo user IsActive=true/IsLockedOut=false/LockoutDate=now/Language="vi", email chuẩn hoá lowercase, hồ sơ Trim) + record RegistrationResult (Services/RegistrationService.cs); đăng ký DI (Program.cs); API POST /api/v1/register (ApiV1Controller + RegisterReq); admin UI RegisterController + Views/Register/Index.cshtml (form đăng ký + bảng quy tắc) + nav "Tự đăng ký" (_Layout); Seeder thêm DDL Postgres (ALTER TABLE Users ADD COLUMN IF NOT EXISTS PhoneNo/Language). Build Release: 0 error (2 warning cũ không liên quan: AuthControllers db unread, SessionService scope unread). Test: 189/189 pass (11 test mới cho RegistrationServiceTests: tạo user hoạt động + mật khẩu băm, chuẩn hoá email lowercase, Trim họ tên/số điện thoại, từ chối email rỗng, từ chối email đã tồn tại, từ chối mật khẩu rỗng, từ chối mật khẩu quá ngắn, nhận đơn vị đang hoạt động, từ chối đơn vị không tồn tại, từ chối đơn vị bị khoá, trả về Id người dùng mới). Commit 217fbb4 trên main (đã ở origin/main). Chỉ sửa file trong MiniSSO, không sửa nguồn.
