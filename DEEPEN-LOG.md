# DEEPEN-LOG — MiniSSO

- 2026-XX-XX: BLOCKED — terminal delegation không khả dụng trong phiên này (mọi lệnh PowerShell/cmd/git-bash đều trả "Terminal delegation failed: terminated", không có output). Không thể đọc nguồn D:\idocNet\2017.A.iNOS.InBrand (read tool 403 ngoài workspace, glob chặn path ngoài workspace), không thể chạy dotnet build verify, không thể git commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry): BLOCKED lại — terminal delegation vẫn hỏng (thử PowerShell/cmd/git-bash, foreground/background, nhiều tên shell: tất cả "Terminal delegation failed: terminated", 0 output). Không đọc được nguồn ngoài workspace, không build verify, không git. Không port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 3): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo`, `Write-Output`, `Get-Location`, `cmd /c`, `& 'C:\Program Files\Git\bin\bash.exe' -lc ...`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 4): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo`, `Write-Output`, `Get-Date`, `whoami`, `cmd /c echo`, `$PSVersionTable`, `& 'C:\Program Files\Git\bin\bash.exe' -lc "echo probe"`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-XX-XX (retry 5): BLOCKED lại — terminal delegation vẫn hỏng hoàn toàn (thử `echo hello`, `Write-Output "test-ok"`, `Get-Location`, `cmd /c echo cmd-ok`, `$PSVersionTable.PSVersion.ToString()`, `& 'C:\Program Files\Git\bin\bash.exe' -lc "echo bash-ok"`: tất cả trả "Terminal delegation failed: terminated", 0 output). read tool vẫn 403 ngoài workspace (D:\idocNet\2017.A.iNOS.InBrand), glob vẫn chặn path ngoài workspace. Không đọc được nguồn iNOS.InBrand → không xác định được nghiệp vụ để port; không build verify; không git add/commit/push. Chưa port nghiệp vụ nào. Không over-claim.
- 2026-09-23: DONE — terminal đã hoạt động lại. Đọc nguồn iNOS.InBrand qua SVN working-copy (Dev/Dev20/Docs chỉ có .svn; đọc file từ pristine store bằng `_svndump` sau khi sửa bug đường dẫn pristine: tên file = full sha1, không tách 2 ký tự đầu). Nghiệp vụ port: **RBAC theo nhóm** (Sys_User → Sys_UserInGroup → Sys_Group → Sys_Access → Sys_Object) — MiniSSO trước chỉ có Roles csv phẳng. Thêm entities Group/GroupMember/PermissionObject/GroupAccess (Domain/Entities.cs), DbSets+index (Data/AppDbContext.cs), RbacService (quyền hiệu lực = hợp đối tượng quyền qua các nhóm đang hoạt động), seed 3 nhóm + 5 đối tượng quyền + cấp quyền + thành viên (Data/Seeder.cs, kèm DDL Postgres), API /api/v1/groups|objects|groups/{id}/access|members|users/{id}/permissions (ApiV1Controller), trang admin /Group (GroupController + Views/Group/Index.cshtml + nav). Build Release: 0 error (1 warning cũ không liên quan). Chạy thật + test: admin→SYSADMIN→5 quyền, sales→SALES→1 quyền; create group→grant→add member→quyền hiệu lực cập nhật đúng. Commit 2fe9f54 trên main (đã ở origin/main). Chỉ sửa file trong MiniSSO, không sửa nguồn.
- 2026-09-23 (retry 6): DONE — đọc nguồn iNOS.InBrand qua SVN pristine (Dev/.svn/wc.db + _svndump). Nghiệp vụ port: **Bảo mật tài khoản / khoá đăng nhập** (SysUser: Enable / Lockout / LockoutDate / VerificationCode + Sys_User_Login + ResetPass). MiniSSO trước chỉ có IsActive, không đếm sai, không khoá. Thêm: AppUser.FailedLoginCount/IsLockedOut/LockoutDate/LockoutUntil/VerificationCode/LastLoginAt (Domain/Entities.cs) + entity LoginAttempt (nhật ký đăng nhập) + DbSet/index (Data/AppDbContext.cs); AccountSecurityService (đếm sai liên tiếp, tự khoá sau 5 lần sai trong 15 phút, tự mở khi hết hạn, admin lock/unlock/reset-password, ghi log mỗi lần thử); AuthService.ValidateUserAsync + AccountController.Login dùng service (hiện lý do cụ thể: vô hiệu/khoá/sai); API /api/v1/users/{id}/lock|unlock|reset-password + /api/v1/login-attempts + mở rộng GET /users (ApiV1Controller); admin UI UserController Lock/Unlock/ResetPassword + Views/User/Index.cshtml (cột Bảo mật + nút); Seeder thêm DDL Postgres (ALTER TABLE Users ADD COLUMN IF NOT EXISTS ... + bảng LoginAttempts). Build Release: 0 error (1 warning cũ không liên quan). Test: 15/15 pass (8 test mới cho lockout: đếm sai, khoá sau ngưỡng, từ chối khi khoá/vô hiệu, admin mở khoá, reset mật khẩu, ghi log). Chỉ sửa file trong MiniSSO, không sửa nguồn.
